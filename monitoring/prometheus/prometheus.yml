global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus monitors itself
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Scrape the Next.js app (standard HTTP probe)
  - job_name: "quiz-app"
    metrics_path: /
    scheme: http
    static_configs:
      - targets: ["quiz-app:3000"]
